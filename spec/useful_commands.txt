# Create conda environnement 
conda init tai-env

# Activate virtual environnement
conda activate tai-env

# Download requirements (inside env) 
pip install -r requirements.txt --no-deps  # First pass without dependencies
pip install -r requirements.txt  # Second pass to get dependencies