// Filters service removed: application now only displays OD pairs on the map.
// This file kept as a minimal stub to avoid build-time import errors if any
// code still imports `Filters`. Remove the import sites if you remove this file.
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class Filters {
  // Stub: no-op service. Using this will have no effect.
}
